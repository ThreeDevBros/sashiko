import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.franchisefeasthub',
  appName: 'franchise-feast-hub',
  webDir: 'dist'
};

export default config;
